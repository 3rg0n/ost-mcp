[package]
name = "mailbox"
description = "The Mailbox trait and shared types every backend (OST/PST, Mac Outlook, .olm) implements"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
